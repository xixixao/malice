module.exports = (OMeta, OMLib) -> parse: (input) ->
  {ometaError} = OMLib

  ometa First
    rule = "The looking glass" -> "Success"
#         | anything+ -> "Failure"

  First.matchAll(input, "rule", undefined, ometaError)